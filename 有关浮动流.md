## 浮动流问题

### 问题复现

```html
<div class="wrapper">
    <div class="content">1</div>
    <div class="content">2</div>
    <div class="content">3</div>
</div>

<style>
    .wrapper{
        border:2px solid black;
    }
    .content{
        float:left;
        width:100px;
        height:100px;
        background-color:black;
        color:#fff;
    }
</style>
```

> 以上例子不难发现父元素无法包裹住浮动的子元素

### 原因分析

这是因为：

- 所有产生了**浮动流**的元素，**块级元素**看不到他们
- 产生了 **BFC** 的元素和**文本类属性(inline)** 的元素以及**文本**都能看到浮动元素

---

### 解决方案

所以想解决这个问题其实非常简单 —— **清除浮动流** 或 **使父元素产生 BFC**。

#### 方案一：清除浮动流

在上面的 `.wrapper` 中设置伪元素：

```css
<style>
    .wrapper{
        border:2px solid black;
    }
    .wrapper::after{
        content="";
        display:block;
        chear:both;
    }
</style>
```

> `chear:both` 是专门用来清除浮动流的，并且这个属性只会在**块盒**中生效。

#### 方案二：使父元素产生 BFC

以下是触发一个盒子 BFC 的属性设置：

- `position: absolute;`
- `display: inline-block;`
- `float: left / right;`
- `overflow: hidden;`

```css
<style>
    .wrapper{
        border:2px solid black;
        float: left; /* 上面四个属性任选一个即可，更具实际情况来进行选择 */
    }
</style>
```

> **值得一提：**
>
> 任一元素设置了 `position: absolute;` 或 `float: left / right;`，会在内部把元素转换为 `inline-block`。
>
> 这就是方案二中设置父元素不会占满一整行的原因。

# 错题本

> 创建时间: 2026-04-25 21:30
> 最后更新: 2026-04-25 21:30
> 总问题数: 2
> 知识点类: 2 条

---

## 🐛 代码错误类问题

（暂无记录）

---

## 📚 知识点类问题

### 问题 #1: 浏览器的布局阶段

**首次记录**: 2026-04-25 21:30
**最近记录**: 2026-04-25 21:30
**记录次数**: 1

**原始问题**:
> 请介绍浏览器的布局阶段，布局树和DOM树的关系是什么？

**知识点**: 浏览器渲染原理 / 布局阶段

**核心解答**:
布局阶段遍历 DOM 树，计算每个节点的几何信息（宽高、位置），生成布局树。布局树和 DOM 树通常不一一对应。

**深入理解**:

布局树和 DOM 树不一致的三类情况：

**1. DOM 节点不生成布局对象**
- `display:none` 的元素（无几何信息，不参与渲染）
- `script`、`style`、`meta` 等非视觉元素

**2. 无 DOM 节点但生成布局对象**
- 伪元素：`::before`、`::after`（有 content 且可见时）
- `::first-line`、`::first-letter`
- 匿名盒：块盒中的纯文本需要匿名行盒包裹

**3. 结构重组**
```html
<div>文本<span>行内</span>文本</div>
```
布局树会生成多个匿名行盒来包裹分散的文本。

**包含块（Containing Block）**：
- 元素定位和尺寸计算的参考框
- 大部分元素：父元素的内容区
- `position:fixed`：视口
- `position:absolute`：最近的定位祖先

**代码示例**:
```html
<!-- 匿名盒示例 -->
<div>
  Some text
  <span>inline element</span>
  More text
</div>

<!-- 布局树结构 -->
Block Box (div)
├── Anonymous Line Box
│   ├── Text: "Some text"
│   └── Inline Box (span)
│       └── Text: "inline element"
└── Anonymous Line Box
    └── Text: "More text"
```

**学习要点**: ⭐ 布局树 ≠ DOM 树，记住三类不一致情况（不生成/无节点生成/结构重组），包含块概念是高频追问点

**高频追问**:
1. 什么是包含块？
2. display:none 和 visibility:hidden 的区别？
3. 回流和重绘什么时候发生？
4. ::first-line 也是伪元素吗？

---

### 问题 #2: reflow（回流）的本质与触发机制

**首次记录**: 2026-04-25 21:45
**最近记录**: 2026-04-25 21:45
**记录次数**: 1

**原始问题**:
> reflow 的本质是什么？为什么修改属性不会立即 reflow，但读取属性会触发立即 reflow？

**知识点**: 浏览器渲染原理 / reflow（回流）

**核心解答**:
reflow 的本质是重新计算布局树。浏览器会批量延迟布局计算以优化性能，但读取布局属性时会强制立即 reflow（强制同步布局）。

**深入理解**:

**触发 reflow 的两类操作：**

**1. 修改几何属性（延迟计算）**
- 尺寸：width、height、padding、margin、border
- 位置：top、left（非 transform）
- 内容：innerHTML、字体大小变化
- 布局：display、position、flex 相关

浏览器不会立即 reflow，而是标记为"脏"，等 JS 执行完统一计算。

**2. 读取布局属性（强制同步布局）**
- offsetTop/Left/Width/Height
- scrollTop/Left/Width/Height
- clientTop/Left/Width/Height
- getComputedStyle()、getBoundingClientRect()

读取时必须返回准确值，所以浏览器被迫立即 reflow。

**代码示例**:
```javascript
// ❌ 布局抖动（读写交替 = 多次强制 reflow）
for (let i = 0; i < 100; i++) {
  box.style.width = box.offsetWidth + 10 + 'px';
  // 修改 → 读取 → 强制 reflow → 循环 100 次 = 100 次 reflow
}

// ✅ 正确写法：读写分离
const width = box.offsetWidth; // 先读取一次
for (let i = 0; i < 100; i++) {
  box.style.width = (width + i * 10) + 'px';
}
// 只有最后 1 次 reflow
```

**学习要点**: ⭐ reflow = 重新计算布局树，修改属性延迟计算，读取属性强制立即计算。布局抖动是性能杀手，解决方案是读写分离。

**高频追问**:
1. 哪些属性会触发 reflow？
2. 什么是布局抖动？如何避免？
3. reflow 和 repaint 的区别？
4. 如何减少 reflow？

---
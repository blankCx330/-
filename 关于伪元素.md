## 伪元素

### 基本概念

```html
<div>
     123
</div>

<style>
    div::before{
        content:""
    }
    div::after{
        content:""
    }
</style>
```

上面的设置在实际情况下就是下面这个样子：

```html
<div>
    ::before
     123
    ::after
</div>
```

> 不难发现伪元素是在**元素内部**的——`::before` 在元素内容的第一位，`::after` 在元素内容的最后一位。

---

### 注意事项

- **伪元素天生就存在**，CSS 中只是**选中**而不是创造
- 伪元素默认为**行盒**，想修改宽高要使用 `display: block;` 改为块盒
- 伪元素的样式中，`content: ""` 是**必须的** —— 因为伪元素无法通过 HTML 代码来控制其元素的内容，所以需要 `content: ""` 来进行设置，即使不需要内容也要设置为空

> `content` 这个属性**只能**在伪元素选择器中使用。
